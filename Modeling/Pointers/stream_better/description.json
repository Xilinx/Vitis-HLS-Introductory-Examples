{
  "name": "stream_better",
  "description": "This example shows an alternative to ap_fifo mode for pointers in the interface and uses ap_hs as the mode instead",
  "hls": {"config": "hls_config.cfg"}
}
